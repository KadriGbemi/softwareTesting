const subtract = (a, b) => {
  return a - b;
};

const sum = (a, b) => {
  return a + b;
};

function showNumberString() {
  return "Numbers";
}

module.exports = { subtract, sum, showNumberString };

// export default { subtract, sum }